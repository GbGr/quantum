struct Movement
{
    FP linear;
    FP angular;
}

component PlayerVehicle
{
  Movement movement;
  EntityRef chassis;
  EntityRef wheel_fl;
  EntityRef wheel_fr;
  EntityRef wheel_rr;
  EntityRef wheel_rl;
}